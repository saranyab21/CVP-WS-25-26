# Core scientific stack used across the Computer Vision Project exercises.
# Tested with Python 3.10+.
numpy>=1.24
scipy>=1.10
matplotlib>=3.7
scikit-learn>=1.3

# Notebook environment
jupyter>=1.0
notebook>=7.0

# Exercise 2 — Demosaicing & HDR (raw imaging)
rawpy>=0.18
Pillow>=10.0

# Exercise 3 — Writer Identification (SIFT + VLAD)
opencv-python>=4.8
tqdm>=4.65
joblib>=1.3

# Exercise 5 — CV in the Humanities (Selective Search + detection)
scikit-image>=0.21
torch>=2.0
torchvision>=0.15

# Additional dependencies will be appended as later exercises are added
# (e.g. scikit-image, torch) — see each exercise README.
